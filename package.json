{
  "name": "skygeni",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --open\"",
    "npm-install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend -- --open"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
